<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.software.fitness.dao.MemberDao">
    <insert id="addMember" parameterType="com.software.fitness.domain.Member">
        insert into coach
        (`password`, `name`, `gender`, `date_of_birth`, `phone_number`, `validity`, `state`, `number_of_courses`)
        VALUES
        (#{password},#{name},#{gender},#{date_of_birth},#{phone_number},#{validity},#{number_of_courses});

        <selectKey keyProperty="order_id" order="AFTER" resultType="java.lang.Integer">
            SELECT last_insert_id();
        </selectKey>
    </insert>
</mapper>